import React from 'react';

/**
 * HeroCamera (Deprecated - Hero video and clouds removed)
 * Retained as a safe no-op component to prevent import breakage.
 */
export default function HeroCamera() {
  return null;
}
